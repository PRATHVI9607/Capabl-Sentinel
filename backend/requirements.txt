# Core API
fastapi==0.115.6
# Hugging Face Spaces offers Docker only on paid plans, so the Space runs under
# the Gradio SDK: space_app.py serves this FastAPI app and mounts a small Gradio
# landing page so the Space URL is not a 404. Unused outside that deployment.
gradio==5.9.1
uvicorn[standard]==0.30.0
python-multipart==0.0.20
pydantic==2.8.2
pydantic-settings==2.4.0
httpx==0.27.0
anyio==4.9.0

# Agent orchestration
langchain-core==0.3.29
langgraph==0.2.61
langchain-google-genai==2.0.8
langchain-groq==0.2.3

# Retrieval. fastembed runs bge-small-en-v1.5 on ONNX Runtime; it is the same
# model as sentence-transformers would load, without the PyTorch dependency.
fastembed==0.4.2
rank-bm25==0.2.2
flashrank==0.2.8
numpy==1.26.4

# NLP / graph
spacy==3.7.5
# spacy 3.7.5's CLI imports click.parser.split_arg_string, which Click 8.2
# deprecated. Without this pin `import spacy` emits a DeprecationWarning.
click==8.1.8
pdfplumber==0.11.1
networkx==3.3

# Persistence
sqlalchemy[asyncio]==2.0.31
aiosqlite==0.20.0
asyncpg==0.29.0
