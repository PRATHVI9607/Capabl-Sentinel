# Architecture audit

Generated by `scripts/codegraph_vault.py` from the CodeGraph index.
Re-run after `codegraph sync .`; do not hand-edit.

**61 modules · 178 internal dependencies · 210 symbols**

Edges below 70% resolution confidence are excluded: those are CodeGraph's dynamic-dispatch guesses, useful for exploration and too noisy for an audit.

---

## Import cycles

None. The module graph is a DAG.

## Layering violations

Dependencies must point down the layer list (Contracts → Providers → Retrieval → Tools → Orchestration → Transport). An edge pointing up inverts control.

None. Every dependency points down or sideways.

## Coupling

### Most depended on (fan-in ≥ 8)

| Module | Dependents | Layer |
|---|---|---|
| `app.config` | 20 | Contracts |
| `app.models` | 18 | Contracts |
| `app.agents.state` | 13 | Orchestration |
| `app.models.output` | 9 | Contracts |
| `app.tools` | 9 | Tools |
| `app.agents.nodes.base` | 8 | Orchestration |

High fan-in on a Contracts or Providers module is the point of the layer. High fan-in higher up the stack means a change there ripples widely.

### Widest reach (fan-out ≥ 8)

| Module | Depends on | Layer |
|---|---|---|
| `app.pipeline` | 11 | Orchestration |
| `app.agents.graph` | 10 | Orchestration |
| `app.agents.nodes` | 9 | Orchestration |
| `app.api.analyze` | 8 | Transport |
| `app.main` | 8 | Transport |

## Modules nothing imports

Entry points and framework-dispatched modules appear here legitimately. Anything else is a candidate for deletion.

None.

## Detected communities vs declared layers

Louvain clustering over the dependency graph, compared with the package layout. A community spanning layers is where the code actually couples regardless of where the files live.

- **Orchestration + Providers + Retrieval + Transport + Unassigned** — `app`, `app.api.analyze`, `app.api.history`, `app.cache.client`, `app.db.crud`, `app.db.models`, `app.db.session`, `app.deps`, `app.ingest.loader`, `app.ingest.preprocessor`, `app.main`, `app.pipeline`, `app.stream`
- **Retrieval + Tools + Transport** — `app.api.health`, `app.rag.bm25_index`, `app.rag.chunker`, `app.rag.compressor`, `app.rag.embedder`, `app.rag.local_store`, `app.rag.qdrant_store`, `app.rag.retriever`, `app.rag.vector_store`, `app.tools.retrieval`
- **Orchestration + Providers + Retrieval + Tools + Transport** — `app.agents.nodes.synthesis`, `app.api.tools`, `app.ingest.classifier`, `app.llm`, `app.tools`, `app.tools.actions`, `app.tools.parsing`, `app.tools.scoring`
- **Orchestration + Tools** — `app.agents.graph`, `app.agents.nodes`, `app.agents.nodes.alert_synthesizer`, `app.agents.nodes.base`, `app.agents.nodes.document_router`, `app.agents.nodes.entity_extractor`, `app.agents.nodes.error_handler`, `app.agents.nodes.incident_parser`, `app.agents.nodes.pattern_detector`, `app.agents.nodes.priority_alert_synthesizer`, `app.agents.nodes.regulatory_auditor`, `app.agents.nodes.risk_scorer`, `app.agents.state`, `app.tools.streaming`
- **Retrieval + Tools** — `app.knowledge_graph.graph`, `app.knowledge_graph.serializer`, `app.tools.patterns`

---

See [[docs/codegraph-vault/_INDEX|the code map]] for the navigable version.
